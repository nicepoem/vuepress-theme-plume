---
title: 文件
createTime: 2026/07/10 15:49:47
permalink: /linux/hmynefle/
---
## ls 列出⽬录内容

- `ls` 列出当前⽬录下的⽂件和⼦⽬录

```Bash
[admin@localhost ~]$ ls
pengyuyan.png  wuyanzu.png  公共  模板  视频  图片  文档  下载  音乐  桌面
```

- `ls -l` 以列表形式（竖向排列）显示详细信息，包括⽂件权限、所有者、⼤⼩和最后修改⽇期

```Bash
[admin@localhost ~]$ ls -l
总用量 5472
-rw-r--r--. 1 root  root  2800514 1月  13 21:40 pengyuyan.png
-rw-r--r--. 1 root  root  2800514 1月   2 22:15 wuyanzu.png
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 公共
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 模板
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 视频
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 图片
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 文档
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 下载
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 音乐
drwxr-xr-x. 2 admin admin       6 12月 24 20:55 桌面
```

- `ls -a` 显示所有⽂件，包括隐藏⽂件（以 . 开头的⽂件）

```Bash
[admin@localhost ~]$ ls -a
.              .bash_profile  .dbus          .mozilla       模板  下载
..             .bashrc        .esd_auth      pengyuyan.png  视频  音乐
.bash_history  .cache         .ICEauthority  wuyanzu.png    图片  桌面
.bash_logout   .config        .local         公共           文档
```

- `ls -lh` 以易读的格式显示⽂件⼤⼩（例如，⽤ K、M、B 表示）

```Bash
[admin@localhost ~]$ ls -lh
总用量 5.4M
-rw-r--r--. 1 root  root  2.7M 1月  13 21:40 pengyuyan.png
-rw-r--r--. 1 root  root  2.7M 1月   2 22:15 wuyanzu.png
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 公共
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 模板
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 视频
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 图片
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 文档
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 下载
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 音乐
drwxr-xr-x. 2 admin admin    6 12月 24 20:55 桌面
```

## cd 切换工作目录

当 Linux 终端(命令行)打开的时候，会默认以用户的 `home` 目录作为当前的工作目。我们可以通过 `cd` 命令，更改当前所在的工作目录。

cd 命令来自英文：Change Directory

- `cd /home` 切换到指定的⽬录

```Bash
[admin@localhost /]$ ls
bin   dev  hello.java  lib    media  opt   root  sbin  sys  usr
boot  etc  home        lib64  mnt    proc  run   srv   tmp  var
[admin@localhost /]$ cd /home
```

- `cd ..` 返回上⼀级⽬录

```Bash
[admin@localhost home]$ ls
admin  test  zwj
[admin@localhost home]$ cd ..
[admin@localhost /]$ 
```

- `cd ~` 切换到当前⽤户的主⽬录

```Bash
[admin@localhost home]$ cd ~
[admin@localhost ~]$ 
```

## pwd 显示当前⽬录

通过 `ls` 来验证当前的工作目录，其实是不恰当的。我们可以通过 `pwd` 命令，来查看当前所在的工作目录。

pwd 命令来自：Print Work Directory

- `pwd` 显示当前⼯作⽬录的绝对路径

```Bash
[admin@localhost home]$ pwd
/home
```

## mkdir 创建新⽬录（文件夹）

英文说明：Make Directory

- `mkdir 目录名称` 创建⼀个新⽬录

```Bash
[admin@localhost ~]$ ls
pengyuyan.png  wuyanzu.png  公共  模板  视频  图片  文档  下载  音乐  桌面
[admin@localhost ~]$ mkdir java
[admin@localhost ~]$ ls
java           wuyanzu.png  模板  图片  下载  桌面
pengyuyan.png  公共         视频  文档  音乐
```

- `mkdir 目录名称1 目录名称2 目录名称3` 创建多个⽬录

```
makdir java mysql python
```

- `mkdir -p 一级目录/二级目录` 创建⼀个新⽬录和⼦⽬录

`-p` 递归创建目录，即如果要创建的目录的父目录不存在，也会一并创建


```Bash
[admin@localhost ~]$ ls
java           wuyanzu.png  模板  图片  下载  桌面
pengyuyan.png  公共         视频  文档  音乐
[admin@localhost ~]$ mkdir -p father/son
[admin@localhost ~]$ ls
father  pengyuyan.png  公共  视频  文档  音乐
java    wuyanzu.png    模板  图片  下载  桌面
[admin@localhost ~]$ cd father
[admin@localhost father]$ ls
son
[admin@localhost father]$ 
```

## rm 删除⽂件或⽬录

- `rm 文件名` 删除⽂件

```Bash
[admin@localhost ~]$ ls
father  pengyuyan.png  公共  视频  文档  音乐
java    wuyanzu.png    模板  图片  下载  桌面
[admin@localhost ~]$ rm wuyanzu.png
rm：是否删除有写保护的普通文件 "wuyanzu.png"？y
[admin@localhost ~]$ ls
father  java  pengyuyan.png  公共  模板  视频  图片  文档  下载  音乐  桌面
[admin@localhost ~]$ 
```

- `rm -r github` 删除⽬录及其所有内容

```Bash
[admin@localhost ~]$ ls
father  java  pengyuyan.png  公共  模板  视频  图片  文档  下载  音乐  桌面
[admin@localhost ~]$ rm -r father
[admin@localhost ~]$ ls
java  pengyuyan.png  公共  模板  视频  图片  文档  下载  音乐  桌面
[admin@localhost ~]$ 
```

- `rm -f file` 强制删除⽂件，不提示
- `rm -rf github` 强制删除⽬录及其所有内容，不提示

## cp 复制⽂件或⽬录

英文说明：Copy

- `cp 源文件 目标文件` 复制⽂件

```
# 复制 test.txt 文件为 test2.txt 文件
cp test.txt test2.txt
```

- `cp -r source target` 递归复制⽬录极其内容。

`-r` 递归复制目录，包括目录下的所有文件和子目录

```
# 复制 test 目录到 test2 目录
cp -r test test2
```

## mv 移动文件或目录

mv 命令可以移动文件或目录，也可以重命名文件或目录。

英文说明：Move

- `mv 源文件 目标文件` 移动⽂件或⽬录，也可以⽤来重命名⽂件

```
mv 源文件 目标文件
```

## cat 查看⽂件内容

- `cat file` 显示⽂件内容

```bash
[admin@localhost ~]$ cat 1.txt
	    水调歌头
		苏轼·北宋
	丙辰中秋，欢饮达旦，大醉，作此篇，兼怀子由。
	明月几时有？把酒问青天。
	不知天上宫阙，今夕是何年。
	我欲乘风归去，又恐琼楼玉宇，高处不胜寒。
[admin@localhost ~]$ cat 2.txt
	起舞弄清影，何似在人间。
	转朱阁，低绮户，照无眠。
	不应有恨，何事长向别时圆？人有悲欢离合，月有阴晴圆缺，此事古难全。
	但愿人长久，千里共婵娟。
```

- `cat file1 file2` 连接多个⽂件并显示内容

```bash
[admin@localhost ~]$ cat 1.txt 2.txt
	    水调歌头
		苏轼·北宋
	丙辰中秋，欢饮达旦，大醉，作此篇，兼怀子由。
	明月几时有？把酒问青天。
	不知天上宫阙，今夕是何年。
	我欲乘风归去，又恐琼楼玉宇，高处不胜寒。
	起舞弄清影，何似在人间。
	转朱阁，低绮户，照无眠。
	不应有恨，何事长向别时圆？人有悲欢离合，月有阴晴圆缺，此事古难全。
	但愿人长久，千里共婵娟。
```

- `cat > file` 创建⼀个新⽂件并输⼊内容，按 Ctrl + D 保存退出

```bash
[admin@localhost ~]$ ls
1.txt  java           公共  视频  文档  音乐
2.txt  pengyuyan.png  模板  图片  下载  桌面
[admin@localhost ~]$ cat > 3.txt
666[admin@localhost ~]$ cat 3.txt
666[admin@localhost ~]$ 
```

- `cat >> file` 追加内容到⽂件末尾

```bash
666[admin@localhost ~]$ cat >> 3.txt
999[admin@localhost ~]$ cat 3.txt
666999[admin@localhost ~]$ 
```

- `cat file1 file2 > file3` 合并 file1 和 file2 的内容，并将合并后的内容存储到 file3 中

```
[admin@localhost ~]$ ls
1.txt  3.txt  pengyuyan.png  模板  图片  下载  桌面
2.txt  java   公共            视频  文档  音乐
[admin@localhost ~]$ cat 1.txt
	    水调歌头
		苏轼·北宋
	丙辰中秋，欢饮达旦，大醉，作此篇，兼怀子由。
	明月几时有？把酒问青天。
	不知天上宫阙，今夕是何年。
	我欲乘风归去，又恐琼楼玉宇，高处不胜寒。
[admin@localhost ~]$ cat 2.txt
	起舞弄清影，何似在人间。
	转朱阁，低绮户，照无眠。
	不应有恨，何事长向别时圆？人有悲欢离合，月有阴晴圆缺，此事古难全。
	但愿人长久，千里共婵娟。
[admin@localhost ~]$ cat 1.txt 2.txt 3.txt
	    水调歌头
		苏轼·北宋
	丙辰中秋，欢饮达旦，大醉，作此篇，兼怀子由。
	明月几时有？把酒问青天。
	不知天上宫阙，今夕是何年。
	我欲乘风归去，又恐琼楼玉宇，高处不胜寒。
	起舞弄清影，何似在人间。
	转朱阁，低绮户，照无眠。
	不应有恨，何事长向别时圆？人有悲欢离合，月有阴晴圆缺，此事古难全。
	但愿人长久，千里共婵娟。
```

## vim 编辑⽂件

`vim file` 编辑⽂件

`i` 切换到插⼊模式

`Esc` 退出插⼊模式

`:w` 保存⽂件

`:q` 退出⽂件

`:wq` 保存并退出⽂件

`:q!` 强制退出⽂件

## chmod 修改⽂件权限

Linux 中的权限可以应⽤于三种类别的⽤户：

- ⽂件所有者（u）

- 与⽂件所有者同组的⽤户（g）

- 其他⽤户（o）

### 1、符号模式

符号模式使⽤字⺟来表示权限，如下：

- 读（r）

- 写（w）

- 执⾏（x）
- 所有（a）

例如：

- `chmod u+w file` 给⽂件所有者添加写权限

- `chmod g-r file` 移除组⽤户的读权限
- `chmod o+x file` 给其他⽤户添加执⾏权限
- `chmod u=rwx,g=rx,o=r file` 设置⽂件所有者具有读写执⾏权限，组⽤户具有读执⾏权限，其他⽤户具有

读权限

### 2、数字模式

数字模式使⽤三位⼋进制数来表示权限，每位数字代表不同的⽤户类别（所有者、组、其他⽤户），数字是其各⾃

权限值的总和：

- 读（r）= 4
- 写（w）= 2
- 执⾏（x）= 1

权限模式可以是从 0（⽆权限）到 7（读写执⾏权限）的任何值。

- `chmod 755 file` 使得⽂件所有者有读写执⾏（7）权限，组⽤户和其他⽤户有读和执⾏（5）权限。
- `chmod 644 file` 使得⽂件所有者有读写（6）权限，⽽组⽤户和其他⽤户只有读（4）权限。

使⽤ chmod +x test.sh 或者 chmod 755 test.sh 来给⽂件添加可执⾏权限。

## zip ⽂件压缩

### gzip

1、压缩⽂件

- `gzip filename` 将⽂件压缩成 `.gz` 格式，原⽂件会被压缩后的⽂件替换

```
[admin@localhost ~]$ ls
1.txt  3.txt  pengyuyan.png  模板  图片  下载  桌面
2.txt  java   公共            视频  文档  音乐
[admin@localhost ~]$ gzip 1.txt
[admin@localhost ~]$ ls
1.txt.gz  3.txt  pengyuyan.png  模板  图片  下载  桌面
2.txt     java   公共            视频  文档  音乐
[admin@localhost ~]$ 
```

- `gzip -k filename` 压缩⽂件但保留原⽂件

2、解压⽂件

`gzip -d filename.gz` ：解压 `.gz` 格式的⽂件

```
[admin@localhost ~]$ ls
1.txt.gz  3.txt  pengyuyan.png  模板  图片  下载  桌面
2.txt     java   公共            视频  文档  音乐
[admin@localhost ~]$ gzip -d 1.txt.gz
[admin@localhost ~]$ ls
1.txt  3.txt  pengyuyan.png  模板  图片  下载  桌面
2.txt  java   公共            视频  文档  音乐
[admin@localhost ~]$ 
```

### zip

1、压缩⽂件或⽬录

- `zip -r archive_name.zip directory_or_file` ：压缩⽂件或⽬录到 .zip 格式



2、解压

 `unzip archive_name.zip` 解压 .zip 格式的⽂件



### tar

**tar**

tar 是 Tape ARchive（磁带归档）的缩写，它是一个在Unix/Linux系统中用于将多个文件打包成一个归档文件的工具。虽然名称中有"磁带"，但现在的 `tar` 主要用于文件打包，而不是磁带备份。

- `c` 创建⼀个新的 tar 包

- `z` 使⽤ gzip 压缩
- `v` 显示详细信息
- `f` 指定⽂件名

## **tree** 显示⽬录结构

`tree` 以树形结构显示⽬录

`tree -L 2` 只显示两层⽬录

`tree -d` 只显示⽬录

`tree -a` 显示所有⽂件和⽬录

`tree -h` 以易读的格式显示⽂件⼤⼩
