---
title: 远程连接
createTime: 2026/07/10 15:49:47
permalink: /linux/dxjtnr7e/
---
# xshell


## ubuntu配置远程登录

Ubuntu 桌面版通常没有预装 SSH 服务端，需要手动安装。

更新软件包列表

```bash
sudo apt update
```

1. 安装 `openssh-server`

```bash
sudo apt install openssh-server
```

安装完成后，SSH 服务通常会自动启动。

2. 设置开机自启并立即启动

```bash
sudo systemctl enable --now ssh 
```

3. 查看服务状态

```bash
sudo systemctl status ssh
```

正在运行中（Active）

```bash
● ssh.service - OpenBSD Secure Shell server
     Loaded: loaded (/usr/lib/systemd/system/ssh.service; enabled; preset: enabled)
     Active: active (running) since Fri 2026-07-03 22:15:04 CST; 10min ago
 Invocation: 77b51340c41b4d00bb14aa37a450049c
TriggeredBy: ● ssh.socket
       Docs: man:sshd(8)
             man:sshd_config(5)
    Process: 11327 ExecStartPre=/usr/sbin/sshd -t (code=exited, status=0/SUCCESS)
   Main PID: 11330 (sshd)
      Tasks: 1 (limit: 8794)
     Memory: 4.1M (peak: 5.9M)
        CPU: 112ms
     CGroup: /system.slice/ssh.service
             └─11330 "sshd: /usr/sbin/sshd -D [listener] 0 of 10-100 startups"

```

